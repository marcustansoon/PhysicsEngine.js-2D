Client:
sendData: chef(action,timestamp,data)

Host:
sendData: chef(action,timestamp,data)


HOST/Client(s):
onReceived: chef(action,timestamp,data)

if(action=move)
 time=time needed to move toward data.to based on timestamp
 chef.moveTo(from:data.from, to:data.to, time:time)

if(action=chop) // chopping burger 
 time=progress time needed based on timestamp
 chef.chop(time)

onReceived: cookingPot(action,timestamp,data)
 if(action=cooking)
 time=time needed to cook data.foodType from timestamp
cookingPot.cook(data.foodType, time)


client tap on cookingPot
client send cookingPot(action,timestamp,data)
host receive cookingPot(action,timestamp,data)
host process cookingPot(action,timestamp,data)
host send cookingPot(action,timestamp,data)
client process cookingPot(action,timestamp,data)


 

client tap on water
client send chef(action,data) // action=grab-water;data.id=waterid
host receive and process  chef(action,data)
host send chef(action,timestamp,data)
client receive and process chef(action,timestamp,data)


new customer(s) coming in
host send customer(action:new-c,timestamp,data:redshirt;group1)
host send customer(action:new-c,timestamp,data:blueshirt;group1)
host send customer(action:c-move,timestamp,data:group1;fromXY;toXY)





action:{
 players:[chef1{
		'action':chop,timestamp:,data
		}, chef2],
 customersGroupID:{group1:[],group2:[])

}


player -> grab mango -> walk -> chop on table -> mango slices on table -> take mango slices


mango: 0 -> raw
1-> chopped

client send:
action:'carry-new-food', type:mango, holderId:player-1
server send:
action:'carry-new-food', type:mango, id:mango-id1, holderId:player-1 (clients will create new mango sprite and show player1 carrying a mango anim )



client send:
action:'carry-move-to', id:mango-id1, holderid:player-1, toXY
server send:
action:'carry-move-to', id:mango-id1, holderid:player-1, toXY, timestamp, fromXY

client send:
action:chop,id:mango-id1,holderid:player-1,tableid:table-1
server send:
action:chop,id:mango-id1,holderid:player-1:tableid:table-1, timestamp //start time
->action:chop,playerid:player-1
->action:chop,tableid:table-1,foodid:mango-1
Upon receving:
client locks player-1 (show chopping anim)
client show progress bar on table-1 ( mango anim)

on done:
server send:
action:chop-done,id:mango-id1,holderid:player-1:tableid:table-1,timestamp
upon receiving it
client show chopped mango on table-1 (change sprite of mango-id1 & change state to chopped)
client stop progress bar
client stop player

player should has reset method to reset current anim
anim: carry,walk,idle
holding obj:obj id
table should has reset method (rm progress)
holding obj

during outofsync, send objs and states to that player
=

client -> server send multiple times with same seq
server validate and only process the first seq

states = {
 id1:{type:player,state:{action:chop,timestamp:123,data.food:mango}} //player 1
 id2:{type:player,state:{action:moveTo,timestamp:123,data.from:..data.to:...}}
 id3:{type:player,state:{action:moveTo-carry,timestamp:123,data.from:..data.to:..,data.id:id5}}
 id4:{type:table,state:{..}}
 player-id1:action:chop,id:mango-id1,tableid:table-1, timestamp //start time
}

foods = {
 id5:{type:mango,state0}

}

before (outofsync):
objs = {
	player1:holding:false
}

after:
objs = {
	player1:holding:true, objid:mango-id1
}



